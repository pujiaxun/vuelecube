const DEFAULT_CONFIGS = {
  cubeType: '333',
  needScramble: true,
  startDelay: 400,
  fireKeyCode: 32,
  autoArchiveSessionThreshold: 12,
  needAutoArchiveHint: true,
  themeStyle: 'dark',

  // Readonly
  maxSessionSolves: 30,
  minSessionSolves: 3,
  maxStartDelay: 3000,
  minStartDelay: 0,
};

export default DEFAULT_CONFIGS;
